// Single source of truth is config/docTypes.js — re-exported here only so any
// existing `from '../constants/disciplines'` imports don't break.
export { DEFAULT_DISCIPLINES, getDisciplines, saveDisciplines } from '../config/docTypes'
